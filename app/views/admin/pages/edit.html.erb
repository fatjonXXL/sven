<% content_for :title do %>
	Editace stánky "<%= @page.title %>"
<% end %>

<% form_for :page, @page, :url => admin_page_path(@page), :html => { :multipart => true, :method => :put } do |@f| %>
	<%= render :partial => 'admin/pages/form' %>
<% end %>