<div>
	Název:
	<%= @f.text_field :name, { :size => 60 } %><br />
</div>

<div>
	Obsah:<br />
	<%= @f.text_area :content, { :rows => 20, :style => 'width: 100%;' } %>
</div>

<div class="small">
	<%= @f.submit 'uložit', { :class => "small" } %>&nbsp;<%= @f.submit 'uložit a pokračovat', { :name => "save_and_continue", :class => "small" } %> nebo <%= link_to "zpět", admin_templates_url %>
</div>