<div>
	<table>
		<tr>
			<td>Název:</div></td>
			<td><%= @f.text_field :name %></td>
		</tr>
		<tr>
			<td>Typ obsahu:</td>
			<td><%= @f.text_field :content_type %></td>
		</tr>
		</tr>
	</table>
</div>
<div class="cleaner"></div>

<div>
	Obsah:<br />
	<%= @f.text_area :content, { :rows => 20, :style => 'width: 100%;' } %>
</div>

<div class="small">
	<%= @f.submit 'uložit', { :class => "small" } %>&nbsp;<%= @f.submit 'uložit a pokračovat', { :name => "save_and_continue", :class => "small" } %> nebo <%= link_to "zpět", admin_layouts_url %>
</div>